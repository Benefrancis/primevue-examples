export default class Regiao {
  id: number;
  nome: string;

  // Você pode adicionar um construtor, se desejar
  constructor(id: number, nome: string) {
    this.id = id;
    this.nome = nome;
  }
}
